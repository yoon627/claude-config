interface:
  display_name: "Jira Task Notes"
  short_description: "Preview and post concise Jira task work summaries"
  default_prompt: "Use $jira-task to preview a concise work summary for the current Jira task."
