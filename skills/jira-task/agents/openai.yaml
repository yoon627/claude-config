interface:
  display_name: "Jira Task Notes"
  short_description: "Preview a concise Jira task summary"
  default_prompt: "Use $jira-task to preview what was added or changed on the current Jira task; post only after explicit approval."
