name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Node syntax check
        run: |
          node --check statusline.js
          node --check subagent-statusline.js
          node --check codex-quota-refresh.js
          node --check scripts/notify-hook.js
          node --check scripts/dlc-ledger.js
          node --check scripts/dlc-task-router.js
          node --check scripts/dlc-task-router.test.js
          node --check scripts/dlc-evidence-ledger.js
          node --check scripts/dlc-early-stop.js
          node --check scripts/dlc-early-stop.test.js
          node --check scripts/dlc-doc-drift.js
          node --check scripts/dlc-doc-drift.test.js
          node --check scripts/dlc-signal.js
          node --check scripts/dlc-signal.test.js
          node --check scripts/dlc-evidence-ledger.test.js
          node --check scripts/guard-worktree-edit.js
          node --check scripts/guard-worktree-edit.test.js
          node --check scripts/session-brief.js
          node --check scripts/session-brief.test.js
          node --check scripts/session-fetch.js
          node --check scripts/session-fetch.test.js
          node --check scripts/hook-cwd.js
          node --check scripts/session-start-pull.test.js
          node --check scripts/usage-count.js
          node --check scripts/usage-count.test.js
          node --check scripts/install-hooks.test.js
          node --check scripts/plan-lint.js
          node --check scripts/plan-lint.test.js
          node --check scripts/native-overlap-lint.js
          node --check scripts/native-overlap-lint.test.js

      - name: Unit tests
        run: |
          bash scripts/bootstrap/install-codex-skill.test.sh
          bash skills/jira-worklog/test_launcher.sh
          node scripts/dlc-doc-drift.test.js
          node scripts/dlc-early-stop.test.js
          node scripts/dlc-task-router.test.js
          node scripts/dlc-signal.test.js
          node scripts/dlc-evidence-ledger.test.js
          node scripts/guard-worktree-edit.test.js
          node scripts/session-brief.test.js
          node scripts/session-fetch.test.js
          node scripts/session-start-pull.test.js
          node scripts/usage-count.test.js
          node scripts/install-hooks.test.js
          node scripts/plan-lint.test.js
          node scripts/native-overlap-lint.test.js

      - name: Python unit tests
        run: |
          python3 skills/jira-task/test_jira_task.py
          python3 skills/jira-worklog/test_session_time.py
          python3 skills/jira-worklog/test_worklog_scope.py
          python3 skills/jira-worklog/test_register_gate.py

      - name: Plan integrity (changed plans, non-blocking)
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          git fetch --no-tags --depth=1 origin "+refs/heads/$GITHUB_BASE_REF:refs/remotes/origin/$GITHUB_BASE_REF" || true
          git diff --name-only "origin/$GITHUB_BASE_REF" HEAD -- 'plans/*/*-plan.md' \
            | xargs node scripts/plan-lint.js

      - name: JSON validation
        run: |
          node -e "JSON.parse(require('fs').readFileSync('settings.json','utf8'))"

      - name: Shell script lint (shellcheck)
        run: shellcheck scripts/*.sh scripts/bootstrap/*.sh skills/jira-worklog/*.sh skills/e/*.sh skills/improve/*.sh
